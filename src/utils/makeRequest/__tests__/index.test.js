jest.mock('axios');

describe('makeRequest', () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  xit('should make API call with appropriate request options and return response body when only endpoint is specified', async () => {
  });
});
